name: Test Production Lock

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test production lock behavior
        shell: bash
        run: bash deployment/test-production-lock.sh
