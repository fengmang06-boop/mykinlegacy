name: Codex Etsy Growth V2 Experiment Evidence

on:
  push:
    branches:
      - codex/etsy-token-refresh-persistence
    paths:
      - ".github/workflows/codex-etsy-growth-v2-experiment-evidence.yml"
      - "scripts/export-etsy-growth-v2-experiment-evidence.cjs"

permissions:
  contents: write

concurrency:
  group: mensskull-etsy-growth-v2-experiment-evidence
  cancel-in-progress: false

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PROD_SSH_HOST: ${{ secrets.PROD_SSH_HOST }}
      PROD_SSH_USER: ${{ secrets.PROD_SSH_USER }}
      PROD_SSH_PORT: ${{ secrets.PROD_SSH_PORT }}
      PROD_SSH_PRIVATE_KEY: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure SSH
        shell: bash
        run: |
          : "${PROD_SSH_HOST:?Missing PROD_SSH_HOST}"
          : "${PROD_SSH_USER:?Missing PROD_SSH_USER}"
          : "${PROD_SSH_PORT:?Missing PROD_SSH_PORT}"
          : "${PROD_SSH_PRIVATE_KEY:?Missing PROD_SSH_PRIVATE_KEY}"
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          python3 - <<'PY'
          import base64, os, sys
          from pathlib import Path
          raw = os.environ["PROD_SSH_PRIVATE_KEY"].strip()
          key = raw.replace("\\r", "").replace("\r", "").replace("\\n", "\n")
          if "BEGIN " not in key:
              try:
                  key = base64.b64decode(raw).decode("utf-8").replace("\\r", "").replace("\r", "").replace("\\n", "\n")
              except Exception:
                  pass
          if "BEGIN " not in key:
              print("Invalid private key", file=sys.stderr)
              sys.exit(1)
          Path.home().joinpath(".ssh", "prod").write_text(key if key.endswith("\n") else key + "\n")
          PY
          chmod 600 ~/.ssh/prod
          ssh-keyscan -p "$PROD_SSH_PORT" -H "$PROD_SSH_HOST" >> ~/.ssh/known_hosts

      - name: Export experiment files without Etsy calls
        shell: bash
        run: |
          mkdir -p codex_outputs/growth-v2/mission-001
          APP_DIR="$(ssh -i ~/.ssh/prod -o BatchMode=yes -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p "$PROD_SSH_PORT" "$PROD_SSH_USER@$PROD_SSH_HOST" 'pm2 jlist | node -e '\''let d="";process.stdin.on("data",c=>d+=c);process.stdin.on("end",()=>{const a=JSON.parse(d||"[]");const p=a.find(x=>x.name==="mensskull-etsy-ai-manager");if(!p?.pm2_env?.pm_cwd)process.exit(2);process.stdout.write(p.pm2_env.pm_cwd)})'\''')"
          ssh -i ~/.ssh/prod -o BatchMode=yes -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes -p "$PROD_SSH_PORT" "$PROD_SSH_USER@$PROD_SSH_HOST" \
            "cd '$APP_DIR' && node -" \
            < scripts/export-etsy-growth-v2-experiment-evidence.cjs \
            > codex_outputs/growth-v2/mission-001/production-experiment-evidence.json
          node -e 'const fs=require("fs");const p="codex_outputs/growth-v2/mission-001/production-experiment-evidence.json";const d=JSON.parse(fs.readFileSync(p,"utf8"));if(d.safety?.production_writes!==0||d.safety?.etsy_api_calls!==0)throw new Error("unsafe export");console.log({files:d.file_count})'

      - name: Commit export
        shell: bash
        run: |
          git config user.name "codex-analysis-bot"
          git config user.email "codex-analysis-bot@users.noreply.github.com"
          git add codex_outputs/growth-v2/mission-001/production-experiment-evidence.json
          git commit -m "Export Etsy experiment evidence for Growth V2" || exit 0
          git push
